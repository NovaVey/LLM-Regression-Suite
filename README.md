# LLM-Regression-Suite
Catch prompt and model regressions in CI before your users do. Paired evaluation with bootstrap confidence intervals, validated LLM judges, and a PR comment that shows exactly which cases broke — plus a reported false-positive rate, so you know when it's just noise.
